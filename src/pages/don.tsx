import { FunctionComponent } from "react";
import CheckoutForm from "../components/CheckoutForm";

const Don: FunctionComponent = () => {
    return (
        <div>
          <center>
          <h1>Faire un don</h1>
          </center>
          <CheckoutForm />
        </div>
    )
};

export default Don;
